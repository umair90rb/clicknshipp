import dashboard from './dashboard';
import order from './order';
import user from './user';
import store from './store';
import setting from './setting';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, order, store, user, setting]
};

export default menuItems;
