export const servicesName = ['Leapard', 'Deawoo', 'PostEx', 'TCS', 'Call Courier', 'Trax'];
export const serviceValue = ['leopard', 'deawoo', 'postex', 'tcs', 'callcourier', 'trax'];

const SERVICES = servicesName.map((name, index) => [name, serviceValue[index]]);
export default SERVICES;
